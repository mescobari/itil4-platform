# Frontend (Vite) — copia este archivo a `.env` localmente.
# El `.env` no se commitea; este `.env.example` sí (sin secretos).

# URL del backend en dev (en prod queda vacío, lo define .env.production)
VITE_API_URL=http://localhost:3001
